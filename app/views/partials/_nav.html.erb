<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <div class="navbar-item has-dropdown is-hoverable">
        <a class="navbar-link">
          Make
        </a>

        <div class="navbar-dropdown">
          <%= link_to new_deck_path, class: "navbar-item" do %>
            Deck
          <% end %>

          <%= link_to new_card_path, class: "navbar-item" do %>
            Card
          <% end %>

          <hr class="navbar-divider">

          <a class="navbar-item">
            Report an issue
          </a>
        </div>
      </div>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          <% if current_user %>
            <div class="navbar-item">
              <%= image_tag(current_user.avatar_url) unless current_user.avatar_url.nil? %>
            </div>

            <%= link_to destroy_user_session_path, class: "button is-primary", method: :post, rel: "nofollow" do %>
              <strong>Logout</strong>
            <% end %>
          <% else %>
            <%= link_to "/users/auth/google_oauth2", class: "button is-primary", method: :post, rel: "nofollow" do %>
              <strong>Sign up • Login with Google</strong>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
